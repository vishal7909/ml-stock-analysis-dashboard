import { createSlice } from '@reduxjs/toolkit';const token=localStorage.getItem('token');const slice=createSlice({name:'auth',initialState:{token:token||null,user:null},reducers:{setToken(s,a){s.token=a.payload;if(a.payload) localStorage.setItem('token',a.payload);else localStorage.removeItem('token')},setUser(s,a){s.user=a.payload},logout(s){s.token=null;s.user=null;localStorage.removeItem('token')}}});export const { setToken,setUser,logout }=slice.actions;export default slice.reducer;