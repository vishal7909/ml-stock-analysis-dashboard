import { query } from '../config/db.js';import bcrypt from 'bcrypt';export async function createUser({ email,password,full_name,gender,mobile_no,signup_type='e' }){const hashed=await bcrypt.hash(password,10);const sql=`INSERT INTO users (email,password,full_name,gender,mobile_no,signup_type) VALUES ($1,$2,$3,$4,$5,$6) RETURNING id,email,full_name,gender,mobile_no,is_email_verified,is_mobile_verified,created_at`;const { rows }=await query(sql,[email,hashed,full_name,gender,mobile_no,signup_type]);return rows[0]}export async function findUserByEmail(email){const { rows }=await query('SELECT * FROM users WHERE email=$1',[email]);return rows[0]||null}