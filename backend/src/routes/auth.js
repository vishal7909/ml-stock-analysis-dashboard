import express from 'express';import { register,login,verifyEmail,verifyMobile } from '../controllers/authController.js';import { registerValidators,handleValidation } from '../middleware/validators.js';const router=express.Router();router.post('/register',registerValidators,handleValidation,register);router.post('/login',login);router.get('/verify-email',verifyEmail);router.post('/verify-mobile',verifyMobile);export default router;