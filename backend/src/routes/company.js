import express from 'express';import multer from 'multer';import { requireAuth } from '../middleware/auth.js';import { registerCompany,getProfile,updateProfile,uploadLogo,uploadBanner } from '../controllers/companyController.js';const router=express.Router();const upload=multer({ storage:multer.memoryStorage() });router.post('/register',requireAuth,registerCompany);router.get('/profile',requireAuth,getProfile);router.put('/profile',requireAuth,updateProfile);router.post('/upload-logo',requireAuth,upload.single('file'),uploadLogo);router.post('/upload-banner',requireAuth,upload.single('file'),uploadBanner);export default router;